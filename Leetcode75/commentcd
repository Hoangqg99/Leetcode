// Merge Strings Alternately(đan chữ)
// save word1, word2 in StringBuilder and create loop for

    // public String mergeAlternately(String word1, String word2) {
    //     StringBuilder result = new StringBuilder();
    //     int len1 = word1.length(), len2 = word2.length();
    //     for (int i = 0; i < word1.length() || i < word2.length(); i++) {
    //         if (i < len1) result.append(word1.charAt(i));
    //         if (i < len2) result.append(word2.charAt(i));
    //     }
    //     return result.toString();
    // }

// Greatest Common Divisor of Strings(Lấy chữ giống nhau trong mảng)

